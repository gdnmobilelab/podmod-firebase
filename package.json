{
  "name": "pushkin-mod",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node -r dotenv/config ./lib",
    "migrate": "pg-migrate up",
    "prepublish": "tsc",
    "production-start": "npm run migrate && NODE_ENV=production node ./lib"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^2.0.0",
    "node-fetch": "^1.6.1",
    "node-pg-migrate": "0.0.10",
    "pg": "^6.1.0",
    "pg-promise": "^5.3.3",
    "restify": "^4.1.1",
    "uuid": "^3.0.1"
  },
  "devDependencies": {
    "typescript": "^1.8.10"
  }
}
